server:
  port: 8085

spring:
  application:
    name: eureka-hystrix-dashboard

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka

feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: '*'






